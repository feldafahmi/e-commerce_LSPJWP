<header class="border-b border-zinc-200 bg-white"><div class="mx-auto flex max-w-7xl items-center justify-between gap-5 px-5 py-5 lg:px-8"><a class="text-2xl font-black tracking-[-0.08em] sm:text-3xl" href="{{ route('home') }}">SHOP.CO</a><nav class="hidden items-center gap-7 text-sm font-medium lg:flex" aria-label="Navigasi utama"><a href="{{ route('home') }}">Beranda</a><a href="{{ route('home') }}#produk">Produk</a><a href="{{ route('home') }}#kategori">Kategori</a></nav><form class="hidden max-w-sm flex-1 md:block" role="search" action="{{ route('home') }}" method="GET"><label class="sr-only" for="product-search">Cari produk</label><div class="flex items-center gap-2 rounded-full bg-zinc-100 px-4 py-2.5 text-zinc-500"><svg class="size-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="6"/><path d="m20 20-4-4"/></svg><input id="product-search" name="q" value="{{ request('q') }}" class="w-full bg-transparent text-sm outline-none placeholder:text-zinc-400" type="search" placeholder="Cari produk..."></div></form><div class="flex items-center gap-2 text-sm font-semibold">@auth @if (auth()->user()->role === 'pembeli')<a class="rounded-full bg-zinc-100 p-3" href="{{ route('cart') }}" aria-label="Keranjang"><svg class="size-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 4h2l2.4 11.2a2 2 0 0 0 2 1.6h7.8a2 2 0 0 0 1.9-1.4L21 8H6"/><circle cx="10" cy="20" r="1"/><circle cx="18" cy="20" r="1"/></svg></a>@endif<details class="relative"><summary class="flex cursor-pointer list-none items-center gap-2 rounded-full border border-zinc-200 px-3 py-2 hover:bg-zinc-100"><span class="flex size-7 items-center justify-center rounded-full bg-black text-xs text-white">{{ strtoupper(substr(auth()->user()->name, 0, 1)) }}</span><span class="hidden max-w-24 truncate sm:inline">{{ auth()->user()->name }}</span><span aria-hidden="true">⌄</span></summary><div class="absolute right-0 z-30 mt-2 w-56 rounded-2xl border border-zinc-200 bg-white p-2 shadow-xl"><div class="border-b border-zinc-100 px-3 py-2"><p class="truncate text-sm font-bold">{{ auth()->user()->name }}</p><p class="text-xs capitalize text-zinc-500">{{ auth()->user()->role }}</p></div><a class="block rounded-xl px-3 py-2.5 text-sm hover:bg-zinc-100" href="{{ route('profile') }}">Profil saya</a>@if (auth()->user()->role === 'pembeli')<a class="block rounded-xl px-3 py-2.5 text-sm hover:bg-zinc-100" href="{{ route('orders.index') }}">Pesanan saya</a>@else<a class="block rounded-xl px-3 py-2.5 text-sm hover:bg-zinc-100" href="{{ route('seller.products') }}">Panel penjual</a>@endif<form method="POST" action="{{ route('logout') }}">@csrf<button class="w-full rounded-xl px-3 py-2.5 text-left text-sm text-red-600 hover:bg-red-50" type="submit">Keluar</button></form></div></details>@else<a class="rounded-full px-4 py-2.5 hover:bg-zinc-100" href="{{ route('login') }}">Masuk</a><a class="rounded-full bg-black px-4 py-2.5 text-white hover:bg-zinc-700" href="{{ route('register') }}">Daftar</a>@endauth</div></div></header>
