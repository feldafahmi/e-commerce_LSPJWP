<x-layouts.seller title="Kategori | SHOP.CO" active="products">
    <div class="mx-auto max-w-4xl"><div class="flex items-end justify-between"><div><p class="text-sm text-zinc-500">Atur pengelompokan katalog</p><h1 class="mt-1 text-4xl font-black">KATEGORI</h1></div><a class="text-sm underline" href="{{ route('seller.products') }}">Kembali ke produk</a></div>
        @if (session('success')) <p class="mt-5 rounded-xl bg-emerald-50 p-4 text-sm text-emerald-700">{{ session('success') }}</p> @endif
        <form class="mt-8 flex flex-col gap-3 rounded-2xl border border-zinc-200 bg-white p-5 sm:flex-row" method="POST" action="{{ route('seller.categories.store') }}">@csrf<input class="rounded-xl border border-zinc-200 p-3" name="name" placeholder="Nama kategori" required><input class="rounded-xl border border-zinc-200 p-3" name="slug" placeholder="slug-kategori" required><button class="rounded-full bg-black px-5 py-3 text-sm font-bold text-white" type="submit">Tambah</button></form>
        <div class="mt-6 overflow-x-auto rounded-2xl border border-zinc-200 bg-white"><table class="w-full min-w-[600px] text-left text-sm"><thead class="bg-zinc-50 text-xs uppercase text-zinc-500"><tr><th class="px-5 py-4">Nama</th><th class="px-5 py-4">Slug</th><th class="px-5 py-4">Produk</th><th class="px-5 py-4 text-right">Aksi</th></tr></thead><tbody class="divide-y divide-zinc-200">@forelse($categories as $category)<tr><td class="px-5 py-4 font-bold">{{ $category->name }}</td><td class="px-5 py-4">{{ $category->slug }}</td><td class="px-5 py-4">{{ $category->products_count }}</td><td class="px-5 py-4 text-right"><form class="inline" method="POST" action="{{ route('seller.categories.destroy', $category) }}">@csrf @method('DELETE')<button class="text-red-600" type="submit">Hapus</button></form></td></tr>@empty<tr><td class="px-5 py-6 text-center text-zinc-500" colspan="4">Belum ada kategori.</td></tr>@endforelse</tbody></table></div>
    </div>
</x-layouts.seller>
