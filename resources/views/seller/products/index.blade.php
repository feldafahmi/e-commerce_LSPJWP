<x-layouts.seller title="Manajemen Produk | SHOP.CO" active="products">
    <div class="mx-auto max-w-6xl">
        <div class="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p class="text-sm text-zinc-500">Katalog toko Anda</p><h1 class="mt-1 text-4xl font-black tracking-[-0.06em]">MANAJEMEN PRODUK</h1></div><div class="flex gap-3"><a class="rounded-full border border-zinc-200 px-5 py-3 text-sm font-bold" href="{{ route('seller.categories.index') }}">Kategori</a><a class="rounded-full bg-black px-5 py-3 text-sm font-bold text-white" href="{{ route('seller.products.create') }}">＋ Tambah Produk</a></div></div>
        @if (session('success')) <p class="mt-5 rounded-xl bg-emerald-50 p-4 text-sm text-emerald-700">{{ session('success') }}</p> @endif
        <section class="mt-7 overflow-hidden rounded-3xl border border-zinc-200 bg-white"><div class="overflow-x-auto"><table class="w-full min-w-[700px] text-left text-sm"><thead class="bg-zinc-50 text-xs uppercase tracking-wider text-zinc-500"><tr><th class="px-5 py-4">Produk</th><th class="px-5 py-4">Kategori</th><th class="px-5 py-4">Harga</th><th class="px-5 py-4">Stok</th><th class="px-5 py-4 text-right">Aksi</th></tr></thead><tbody class="divide-y divide-zinc-200">@forelse ($products as $product)<tr><td class="px-5 py-4 font-bold">{{ $product->name }}</td><td class="px-5 py-4">{{ $product->category?->name ?? 'Tanpa kategori' }}</td><td class="px-5 py-4">Rp{{ number_format($product->price, 0, ',', '.') }}</td><td class="px-5 py-4">{{ $product->stock }}</td><td class="px-5 py-4 text-right"><a class="mr-3 underline" href="{{ route('seller.products.edit', $product) }}">Ubah</a><form class="inline" method="POST" action="{{ route('seller.products.destroy', $product) }}">@csrf @method('DELETE')<button class="text-red-600" type="submit">Hapus</button></form></td></tr>@empty<tr><td class="px-5 py-8 text-center text-zinc-500" colspan="5">Belum ada produk.</td></tr>@endforelse</tbody></table></div></section>
        <div class="mt-5">{{ $products->links() }}</div>
    </div>
</x-layouts.seller>
